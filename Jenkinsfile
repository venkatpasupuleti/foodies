pipeline {
    agent any
    stages {
        stage('checkout scm'){
            step{
                echo "pulling code from git repo"
            }
        }
        stage('checkout scm'){
            step{
                echo "building project using mvn"
            }
        }
    }
}